import { RippleEffectDirective } from './ripple-effect.directive';

describe('RippleEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleEffectDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
